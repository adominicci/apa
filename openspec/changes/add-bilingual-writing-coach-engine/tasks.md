## 1. Freeze the public seam and review inputs

- [x] 1.1 Confirm before production edits that `@tesina/engine` still exports the canonical `DocLocale` used by essay settings and that planned LT-04 seams can supply one immutable text snapshot, non-negative document-relative `documentStart`, document-absolute protected citation/quotation/source-title/proper-name spans, and an exhaustive typed-message-to-Paraglide adapter. If any seam is false, stop and revise this OpenSpec rather than adding another locale or coordinate contract.
- [x] 1.2 Record the six-category bilingual reviewer rubric, UI-locale rendered-message guidance, exact conservative thresholds, overlap/dedup/order algorithm, protected-source rules, one-to-one evaluator matching algorithm, metric formulas/denominators, supported cells, rounding, and aggregate schema from the approved spec in test-readable fixture metadata. Do not add UI locale, final prose, authorship, confidence, grading, rewrite, or cohort fields to engine input.
- [x] 1.3 Assemble a corpus provenance ledger before adding fixture text. Accept only project-authored MIT-granted material or exact text with a compatible recorded SPDX license and source/attribution; record de-identification and exclude student submissions, personal data, APA-manual text, and unresolved model-output rights.

## 2. Red/green: pure request and range contract

- [x] 2.1 Add failing focused tests for contract-version constants, `DocLocale`, the exact six-category union, a closed message-ID-to-parameter map, public `observedText` plus explanation/question descriptors, and `source: "deterministic"`. Cover the 65,536 UTF-16 bound; non-safe/negative `documentStart`; unsafe `documentStart + text.length`; unsupported language; document-absolute protected spans below/above the snapshot, empty/reversed, or splitting surrogate pairs; and exact slicing after non-BMP text. Confirm the new tests fail because the coach module does not exist.
- [x] 2.2 Create `apps/desktop/src/lib/learning/coach/types.ts` and the smallest pure runtime validation needed to pass those contract tests. Engine input MUST NOT accept UI locale and issues MUST NOT contain final UI strings. Export no confidence, severity, replacement, cohort, reviewer, rule ID, model, editor, persistence, network, telemetry, or quiz type.
- [x] 2.3 Add failing tests for Unicode-aware token/sentence/paragraph offset maps, locale-aware case normalization that preserves Spanish diacritics and `ñ`, eight-token fragment classification, and stable UTF-16 slicing after non-BMP characters. Include `İ` lowercasing to `i` plus combining dot: prove normalized matching maps to the original one-code-unit slice and adds `documentStart` exactly once. Then implement normalized-token-to-original-range mapping and only the shared segmentation primitives required to pass.

## 3. Red/green: protected sources and deterministic normalization

- [x] 3.1 Add failing tests for sorting and merging touching/overlapping validated caller spans by `from`/`to`/kind; retaining kind unions for diagnostics; converting inferred balanced straight/curly quotations and narrow APA-shaped citations from request-relative to document-absolute ranges; unbalanced quotes; masked token counts; same/next-sentence evidence protection; and candidates wholly inside, partially overlapping, or merely touching protection. Then implement the pure mask so every non-empty candidate/protection intersection discards the whole candidate before deduplication, while touching candidates survive; never trim, split, or rebuild a candidate.
- [x] 3.2 Add failing tests that shuffle candidate insertion order and prove exact-duplicate priority/ID collapse; span-length/`from`/`to`/priority/ID ranking; deterministic nested, chained, and bridging same-category selection; touching-span retention; allowed cross-category overlap; fixed six-category output order; and absence of rule IDs/priorities from public issues. Then implement the internal candidate normalizer and make the focused tests pass.

## 4. Red/green: specificity and evidence

- [x] 4.1 Add failing English and Spanish specificity tests for reviewed bounded phrases, competent near-misses, punctuation, capitalization, diacritics, protected source text, proper names, and fragments. Include independent native Spanish examples rather than translated English strings; confirm failure before adding rule data.
- [x] 4.2 Implement the smallest reviewed English/Spanish specificity tables and typed message descriptors that satisfy the tests, preserving only the exact document-language phrase and never producing UI prose or replacement text.
- [x] 4.3 Add failing English and Spanish evidence tests for narrow assertion cues, fewer-than-eight-token fragments, protected citations/quotations in the same or next sentence, support elsewhere beyond that local window, and exact non-accusatory wording that says support or explanation may be needed. Confirm failure before implementation.
- [x] 4.4 Implement the conservative evidence rules and typed descriptors. Keep UI rendering, fact checking, citation correctness, source credibility, whole-document support inference, and claims that text is false or uncited out of the module.

## 5. Red/green: clarity and economy

- [x] 5.1 Add failing clarity tests for the combined English `>45`/Spanish `>50` lexical-token and three-clause-cue thresholds, exact boundary values, long simple sentences, short complex fragments, citations/quotes excluded from counts, punctuation, non-BMP offsets, and native Spanish clause cues. Confirm failure first.
- [x] 5.2 Implement the shared length calculation plus separately reviewed English/Spanish clause cues and typed descriptors; require both thresholds before emitting a clarity issue.
- [x] 5.3 Add failing economy tests for reviewed bounded multiword/syntactic patterns, competent near-misses, isolated intensifiers/transitions/common words, quotations, source titles, proper names, exact phrase ranges, and native Spanish constructions. Confirm failure first.
- [x] 5.4 Implement only the reviewed economy patterns and typed descriptors needed by the tests; do not add broad word bans, UI strings, or automatic deletions/replacements.

## 6. Red/green: repetition and voice

- [x] 6.1 Add failing repetition tests for four-content-token sequences repeated twice within one paragraph, the same sequence split across paragraphs, three-content-token openings across exactly three consecutive sentences, two-opening near-misses, normalized case, Spanish diacritics, protected tokens, overlapping matches, and repeated candidate deduplication. Confirm failure first.
- [x] 6.2 Implement paragraph-bounded phrase and consecutive-opening repetition rules with exact later-occurrence spans, shared stop-token mechanics, native English/Spanish token data, and deterministic candidate output.
- [x] 6.3 Add failing voice tests for the small reviewed English/Spanish formulaic or evasive constructions, competent academic voice, first person, passive voice, transitions, second-language near-misses, fragments, quotations, and cross-category overlap with economy/specificity. Confirm failure first.
- [x] 6.4 Implement native document-language voice patterns and typed descriptors that match only reviewed constructions. Do not infer tone, identity, fluency, authorship, misconduct, or general writing quality.

## 7. Red/green: corpus and offline evaluator

- [x] 7.1 Add failing corpus-schema tests for at least eight fixtures in every English/Spanish by weak/competent/AI-assisted/second-language cell; at least eight proposed positives per document-language/category; immutable IDs; origin/license/source/de-identification fields; proposed exact observations or expected-clean status; and exactly two unassigned `independent-bilingual-reviewer` slots. Confirm the pending schema never invents human IDs, attestations, or decisions, distinguishes proposed from human-agreed support, and cannot pass acceptance before review.
- [x] 7.2 Add the minimum redistributable proposed corpus using the ledger from task 1.3. Keep cohort/reviewer metadata in evaluator-only fixture modules. Prepare evaluation-only English and Spanish UI-locale renderings for every typed message descriptor, enforcing one-sentence/180-code-unit explanation and interrogative-question limits without importing UI locale or rendered prose into the runtime engine.
- [x] 7.3 Add failing one-to-one matcher tests with hand-calculated overlapping fixtures for emitted/expected canonical sorts, category/non-empty-overlap eligibility, exact-span/intersection-length/boundary-distance/emitted-order/expected-ID pair tie-breakers, single-use matching, and stable unmatched emission/expectation counts.
- [x] 7.4 Add failing aggregate tests for stable `writing-coach-evaluation-v1` field/array ordering and deterministic digest; integer half-up basis points; `computed`/`pending-review`/`not-computable` rate states; zero denominators never passing; all zero-emission cells retained; eight-proposed/eight-emitted structural support plus eight-dual-accepted reviewed support; issue micro versus unweighted required-cell macro precision/span/coverage; passage-denominator competent and second-language false positives with separate unmatched-issue diagnostics; and every language/category/cohort/UI-locale aggregation.
- [x] 7.5 Add failing review-linkage tests for stable `{ corpusVersion, fixtureId, issueKey, catalogVersion, uiLocale }` instance keys; recursive lexical-key parameter serialization; specified NUL-separated lowercase SHA-256 render/input digests; two distinct reviewer IDs/attestations; complete independent decisions; pending unassigned/missing/abstained review; failed duplicate/false-attestation/stale/foreign/disagreed review; completed `not-useful` handling; micro/macro usefulness; and aggregate status precedence `failed` over `pending-human-review` over `passed`.
- [x] 7.6 Implement `evaluate.ts` as the smallest deterministic offline matcher/aggregator and add a checked pending aggregate snapshot. Include every fixed cell and raw count, omit timestamps/user text, and add no runtime telemetry or evaluator hook to the application.
- [ ] 7.7 Produce the immutable review handoff bundle. The product owner assigns two distinct opaque IDs to the reviewer slots; each human independently confirms bilingual ability/independence and records every expected-observation and rendered-question decision linked to the exact instance digest. Do not mark this task or acceptance complete while either assignment/decision is missing, duplicated, abstained, stale, or unresolved.
- [ ] 7.8 Run the evaluator after both human decision sets are recorded. Require 85% micro category precision, 75% per supported language/category precision, 90% micro exact-span accuracy, at most 10% competent and second-language false-positive passages per document language, 80% micro dual-reviewer question usefulness, and all support/handoff gates. If a gate fails, narrow/remove the responsible rule or revise the reviewed rendering and create a new digest/review decision; do not weaken, pool, omit, or reuse stale approvals to obtain a pass.

## 8. Red/green: immutable `unslopV1` audit

- [x] 8.1 Add failing English and Spanish audit tests for the exact `unslopV1` identifier and explicit `contentLanguage`; UI-locale feedback differing from document language; exact matches for canned-framing phrases, heading-inflation structures, first-person experiential-claim phrases, reviewed slang tokens/phrases, reviewed nonstandard spelling/grammar patterns, and adjacent repetition; exact UTF-16 violation ranges/codes; competent near-misses; native Spanish patterns; deterministic order; and non-BMP text. Confirm failure before implementation.
- [x] 8.2 Implement `unslopV1.ts` as a pure generated-output pattern audit with independently reviewed bilingual data. Export observable match violations only; do not label intent, call an error deliberate, classify student text, score naturalness/authorship, rewrite, replace, generate, prompt, call a provider, or add a side effect.
- [x] 8.3 Add failing correction-eligibility tests proving: schema-invalid or ungrounded initial drafts are discarded with zero style-correction attempts; only an initially schema-valid, grounded, style-failing draft is eligible for exactly one style-correction attempt; all three validations rerun afterward; any failure discards output; and no second style attempt is authorized. Implement only types and a pure eligibility/transition test helper, and leave whether to invoke correction plus every schema/grounding/generation retry policy to the later generation OpenSpec.

## 9. Boundary and final verification

- [x] 9.1 Add a focused module-boundary test or static check proving coach production imports contain no Svelte/editor mutation, Tauri, network, storage, settings, model/generation, quiz, telemetry, corpus, or evaluator dependency. Confirm no coach module is registered in UI, routes, editor addons, app state, Tauri commands, or persistence.
- [x] 9.2 Run the focused coach suites and aggregate evaluator, then `deno task check`, `deno task test`, `deno fmt --check`, `deno lint`, `git diff --check`, and `openspec validate add-bilingual-writing-coach-engine --strict`. Distinguish any unrelated baseline/environment failure from an LT-03 regression, and report `pending-human-review` honestly until task 7.7 is complete.
- [ ] 9.3 Review the final diff against the single hidden LT-03 PR boundary and the 20-file/1,000-handwritten-line challenge signal. Confirm the engine exposes exactly six categories; every issue has an exact range/source slice and typed explanation/question descriptors; both human reviewer sets and all quality gates pass; analysis uses only document language; runtime feedback rendering remains LT-04/UI-locale work; and no authorship detection, automatic rewrite, UI, editor mutation, Tauri, network, persistence, model, generation, quiz, telemetry, dependency, or visible release promise entered the change.
- [ ] 9.4 When the implementation is ready for its main-bound PR, apply the repository per-change version synchronization and one human CHANGELOG entry that describes only the hidden deterministic foundation, without claiming the writing coach is available or releasing/tagging/publishing anything.
