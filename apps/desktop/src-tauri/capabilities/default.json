{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window: app-data file access, save/open dialogs, and reference-autofill HTTP endpoints. The wildcard http(s) scopes exist because URL autofill scrapes user-pasted pages (http kept for legacy academic sites); the CrossRef/OpenLibrary entries document the primary endpoints.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default",
    "fs:default",
    "fs:allow-appdata-read-recursive",
    "fs:allow-appdata-write-recursive",
    {
      "identifier": "http:default",
      "allow": [
        { "url": "https://api.crossref.org/*" },
        { "url": "https://openlibrary.org/*" },
        { "url": "https://*/*" },
        { "url": "http://*/*" }
      ]
    }
  ]
}
