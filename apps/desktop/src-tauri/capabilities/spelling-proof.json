{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "spelling-proof",
  "description": "Hidden packaged spelling proof: invoke the proof command and exit only.",
  "windows": ["main"],
  "permissions": ["core:default", "process:allow-exit"]
}
